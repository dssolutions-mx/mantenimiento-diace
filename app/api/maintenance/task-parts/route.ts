import { NextResponse } from 'next/server'

// Placeholder route handler to satisfy module/typecheck.
// Implement as needed when the UI/API contract for task-parts is defined.
export async function GET() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 })
}
